extends ../layout
block content

    article
        if board
            h2= title
            = board.content
            div= 'by '+board.name
        else
            h2 게시판 테스트 페이지 입니다.
            | 로그인하면 글 작성할 수 있습니다.
    ul 
        if baord 
            li
                a(href='/board/'+board.idx+'/edit') edit
            li
                a(href='/board/'+board.idx+'/delete') delete
        else
            li 
                a(href='/board/add') add