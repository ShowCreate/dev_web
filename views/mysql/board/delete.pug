doctype html
html 
    head 
        meta(charset='utf-8')
    body 
        h1= 'Delete? '+topic.title
        form(action=`/board/delete/${topic.idx}` method='post')
            p
                input(type='submit' value='YES')
        a(href=`/board/read/${topic.idx}`) no