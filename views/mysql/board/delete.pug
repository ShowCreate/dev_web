doctype html
html 
    head 
        meta(charset='utf-8')
    body 
        h1= row.title
        form(action='/board/delete' method='post')
            p
                input(type='submit' value='YES')
        a(href='/board/read/'+row.idx) no